<% path_to_user = issue.the_editor.id ? user_path(issue.the_editor) : nil %>
<%= render 'users/user_info_link', user: issue.the_editor %>
at&nbsp;<%= issue.updated_at.strftime("%H:%M") %>&nbsp;<%= issue.updated_at.strftime("%d-%b-%Y") %>
