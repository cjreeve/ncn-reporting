$('.default-options').hide();
$('.search-result').remove();
<% @mini_search_results.reverse.each do |result| %>
  $('li.search-option').after("<%= j( render 'site/mini_search', result: result ) %>");
<% end %>
