<%= simple_form_for(@image) do |f| %>
  <%= f.error_notification %>

  <div class="form-inputs">
    <%= f.input :url %>
    <%= f.input :caption %>
    <%= f.association :issue %>
  </div>

  <div class="form-actions">
    <%= f.button :submit %>
  </div>
<% end %>
